---
import { t } from '../i18n/utils';
import type { Locale } from '../i18n/utils';
import { pagePath } from '../i18n/pages';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;

const policyLinks = [
  { label: t(locale, 'footer.privacy'), href: pagePath('privacyPolicy', locale) },
  { label: t(locale, 'footer.cookies'), href: pagePath('cookiePolicy', locale) },
  { label: t(locale, 'footer.aml'), href: pagePath('amlPolicy', locale) },
  { label: t(locale, 'footer.ctf'), href: pagePath('ctfPolicy', locale) },
];

const year = new Date().getFullYear();
---

<footer class="bg-brand-footer text-brand-dark">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      <!-- Company Info -->
      <div>
        <img src="/images/general/kilc-logo-colour-transparent.png" alt="KILC" class="h-36 w-auto mb-4" />
        <p class="text-gray-600 text-sm leading-relaxed">
          Keane International Legal Consultancy
        </p>
        <p class="text-gray-500 text-sm mt-2">
          {t(locale, 'footer.address')}
        </p>
        <a href="mailto:info@kilc.co.uk" class="text-brand-primary hover:text-brand-secondary text-sm mt-1 inline-block transition-colors">
          {t(locale, 'footer.email')}
        </a>
      </div>

      <!-- Quick Links -->
      <div>
        <h3 class="text-sm font-semibold uppercase tracking-wider mb-4 text-brand-dark">
          {t(locale, 'footer.quickLinks')}
        </h3>
        <ul class="space-y-2">
          <li><a href={pagePath('about', locale)} class="text-gray-600 hover:text-brand-dark text-sm transition-colors">{t(locale, 'nav.about')}</a></li>
          <li><a href={pagePath('services', locale)} class="text-gray-600 hover:text-brand-dark text-sm transition-colors">{t(locale, 'nav.services')}</a></li>
          <li><a href={pagePath('markets', locale)} class="text-gray-600 hover:text-brand-dark text-sm transition-colors">{t(locale, 'nav.markets')}</a></li>
          <li><a href={pagePath('fees', locale)} class="text-gray-600 hover:text-brand-dark text-sm transition-colors">{t(locale, 'nav.fees')}</a></li>
          <li><a href={pagePath('contact', locale)} class="text-gray-600 hover:text-brand-dark text-sm transition-colors">{t(locale, 'nav.contact')}</a></li>
          <li>
            <a href="https://platform.kilc.co.uk" target="_blank" rel="noopener noreferrer" class="text-gray-600 hover:text-brand-dark text-sm transition-colors">
              {t(locale, 'nav.xos')} Platform &rarr;
            </a>
          </li>
          <li><a href={pagePath('blog', locale)} class="text-gray-600 hover:text-brand-dark text-sm transition-colors">{t(locale, 'nav.blog')}</a></li>
        </ul>
      </div>

      <!-- Policies & WeChat -->
      <div>
        <h3 class="text-sm font-semibold uppercase tracking-wider mb-4 text-brand-dark">
          {t(locale, 'footer.legal')}
        </h3>
        <ul class="space-y-2">
          {policyLinks.map((link) => (
            <li>
              <a href={link.href} class="text-gray-600 hover:text-brand-dark text-sm transition-colors">
                {link.label}
              </a>
            </li>
          ))}
          <li>
            <button
              id="cookie-settings-btn"
              class="text-gray-600 hover:text-brand-dark text-sm transition-colors cursor-pointer bg-transparent border-none p-0"
            >
              {t(locale, 'footer.cookieSettings')}
            </button>
          </li>
        </ul>
        <script is:inline>
          document.getElementById('cookie-settings-btn')?.addEventListener('click', function () {
            window.dispatchEvent(new CustomEvent('kilc:reset-consent'));
          });
        </script>

        <!-- WeChat QR -->
        <div class="mt-6">
          <p class="text-sm text-gray-500 mb-2">
            {t(locale, 'footer.wechat')}
          </p>
          <img
            src="/images/team/keane-wechat-code.jpg"
            alt="WeChat QR Code"
            class="w-24 h-24 rounded-lg"
            loading="lazy"
          />
        </div>
      </div>
    </div>

    <!-- Partner Logos & Copyright -->
    <div class="mt-12 pt-8 border-t border-gray-300">
      <div class="flex flex-col sm:flex-row items-center justify-between gap-4">
        <div class="flex items-center gap-6">
          <img src="/images/general/british-council-logo.png" alt="British Council" class="h-8 w-auto opacity-60" loading="lazy" />
          <img src="/images/general/ico-logo.png" alt="ICO" class="h-8 w-auto opacity-60" loading="lazy" />
          <img src="/images/general/lanpeng-logo.png" alt="蓝鹏律师事务所" class="h-8 w-auto max-w-[186px] opacity-60" loading="lazy" />
        </div>
        <p class="text-gray-500 text-sm">
          &copy; {year} KILC. {t(locale, 'footer.rights')}
        </p>
      </div>
    </div>
  </div>
</footer>
